stages:
  - build-and-test
  - unit-tests

build-job:
  stage: build-and-test
  image: egymgmbh/jdk11-builder
  script:
    - chmod +x ./gradlew
    - ./gradlew build

unit-testing-job:
  stage: unit-tests
  script:
    - gradle test
  artifacts:
    when: always
    reports:
      junit: build/test-results/test/**/TEST-*.xml
